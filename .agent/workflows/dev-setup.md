---
description: Reset environment and start dev server
---

// turbo-all

1. Remove local node_modules
   `rm -rf node_modules`

2. Install dependencies
   `npm install`

3. Start development server
   `npm run dev`
